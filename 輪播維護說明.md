# 輪播維護說明

輪播已拆成 HTML 文字、空白卡圖與 CSS 裝飾背景。舊的 promo PNG 保留備用，但網站不再使用那些含固定文字的廣告圖。

## 修改廣告文案與展示卡牌

編輯 `promotions-data.js`。每個項目是一頁輪播：

- `id`：唯一識別名稱。
- `theme`：背景配色，支援 `abyss`、`inferno`、`grove`。
- `cardIds`：展示卡牌的 ID 字串，例如 `['25', '13']`。建議每頁兩張。
- `href`：按鈕導向的頁內位置，例如 `#shop` 或 `#library`。
- `copy`：各語言廣告內容，支援 `zh-TW`、`en`、`ja`、`ko`；缺少語言時使用繁中。
- 每個語言有 `eyebrow`（系列標示）、`title`（主標）、`subtitle`（副標）、`description`（說明）、`offer`（優惠文案）、`cta`（按鈕文字）。

存檔後重新整理網頁即可看到修改。可複製一整個項目新增輪播頁，頁數會自動更新。文字可包含一般標點，但 JS 字串內的單引號需要以反斜線跳脫。文字會當作純文字顯示，不執行 HTML。

## 修改卡牌名稱與完整使用規則

輪播與圖鑑共用相同翻譯來源，沒有另外複製卡牌規則。

1. 編輯 `data/card-translations.csv`，依 `card_id` 與 `language` 找到對應列。
2. 修改 `name` 及 `ability_1_title`、`ability_1_text`、`ability_2_title`、`ability_2_text`。技能標題與效果說明都要填寫。
3. 在專案資料夾執行 `node scripts/build-translations.cjs`，更新網站讀取的 `translations-data.js`。
4. 重新整理網站。輪播、圖鑑及詳情會讀取同一份新資料。各語言需分別維護；系統不會自動翻譯修改後的規則。

不要直接修改 `translations-data.js`，因為重建時會被覆寫。既有中文翻譯會優先於 `cards-data.js` 的名稱與規則，因此請依上述 CSV 流程修改。

## 圖片與版型

`cards-data.js` 的 `image` 指向 `cards-clean-layout-31` 空白卡圖；名稱與規則透過共用的 `artwork()` 疊上去。`nameLayout` 與 `rulesLayout` 控制個別卡牌的文字位置。卡图仍保留原本三個阿拉伯數值，這些數字是圖片的一部分；本次可資料化編輯的是名稱、規則與廣告文案。

`promotions.css` 控制背景、文案與卡牌排版。桌面為左右分欄；平板文案在上、兩張卡在下；窄手機為單欄卡牌，避免將規則縮成難以閱讀的小字。背景沒有任何固定文字，改文案不需重生圖片。

`promotions.js` 負責組裝、語言切換與輪播。每 8 秒切換，可在 `setTimeout` 修改時間；滑鼠停留、鍵盤焦點、頁面切到背景或減少動態效果偏好皆有對應處理。點卡牌可看詳情，廣告按鈕是實際連結。

資料使用 JS 檔載入，無須 API，也能直接開啟本機 `index.html`。若未來要接 API，可在更新 `window.PROMOTIONS` 後呼叫 `window.PromotionCarousel.refresh()`。

## 驗證

已驗證 3 頁 × 4 語言 × 3 種視窗寬度（1440、768、390），確認完整技能文字、空白圖來源、卡框文字容納、切換功能及資料修改即時反映。
