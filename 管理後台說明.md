# 管理後台

## 後台介面語言

頂端的「介面語言」可切換繁體中文與 English，涵蓋卡牌、商品、交易查詢、翻譯審核與操作提示。瀏覽器會記住後台的選擇；首次進入時沿用前台的英語偏好，其他語言預設顯示繁體中文。切換不會重新載入頁面或改動查詢條件、卡牌原文、商品名稱與翻譯資料，也不會呼叫翻譯服務。後台文案集中於 `admin-i18n.js`。

## 單一原文與自動翻譯

新增卡牌可選繁體中文、英文、日文或韓文作為原文，填寫名稱、副標題、技能即可。預設勾選「儲存時自動補齊缺少的翻譯」，儲存草稿時會產生另外三種語言。卡牌建立後原文語言固定，避免將既有內容誤當成另一種語言。

展開「翻譯與人工確認」，可切換語言修改內容，並按「確認此語言翻譯」。三種譯文都確認後才可上架；原文有變動會提示待確認，保留所有譯文與人工修改。自動補齊只處理缺少的語言；「重新翻譯目前語言」會先確認是否取代現有內容。儲存草稿時若翻譯服務未設定、失敗或逾時，仍會保存原文與既有譯文，並提示稍後補翻譯；不會因此上架。必填欄位不完整時會顯示欄位提示。系列、圖片、卡牌 ID 與數值不翻譯。

### 啟用 Google Cloud Translation Basic（v2 / NMT）

1. 在 Google Cloud 建立或選擇專案，啟用帳單與 Cloud Translation API。
2. 建立 API key，將 API 限制設為 Cloud Translation API。這是後端請求，不要使用瀏覽器 HTTP referrer 限制。
3. 在 Cloudflare Pages 專案的環境變數／Secrets 加入 `GOOGLE_TRANSLATE_API_KEY`，值為金鑰，使用加密 Secret。Production 與 Preview 分別設定，重新部署後生效。不要把金鑰貼進前端程式、Git 或對話。
4. 用一張草稿測試「自動補齊翻譯」。不再需要 Workers AI binding；既有卡片、譯文及人工確認紀錄可直接沿用，無須 migration。

本機測試可在專案根目錄建立 `.dev.vars`，內容為 `GOOGLE_TRANSLATE_API_KEY="你的金鑰"`，用 `npx wrangler pages dev .` 啟動；此檔已加入 `.gitignore`，不要上傳或提交。正式環境請使用 Pages Secret。

官方文件：[Google 設定步驟](https://docs.cloud.google.com/translate/docs/setup)、[Basic API 金鑰驗證](https://docs.cloud.google.com/translate/docs/authentication)、[定價](https://cloud.google.com/products/translate/pricing)。NMT 每月前 50 萬字元由最高 US$10 抵免涵蓋；帳單仍需啟用，超額會計費。翻成三種語言會分別計算原文字元，重新翻譯也會再次計費。Google Cloud 預算通知不是硬性停用開關；可另外調整 API 用量配額。本程式不會自動重試收費請求。

`POST /api/admin/translate` 只開放有 `card.edit` 權限的登入者，接收 `sourceLanguage`、`source`（name/subtitle/abilities）及 `targets`。後端以純文字送往 Google NMT，每個目標語言獨立請求並行處理，略過空白副標題；依欄位順序重組卡片內容。金鑰只透過後端 HTTP header 傳送，不出現在 URL 或 API 回應。服務檢查譯文欄位、數字與常見佔位符／符號，45 秒逾時或任一語言失敗時整批拒絕，保留原有內容。

未設定金鑰時回傳 `translation-unavailable`，手動翻譯及草稿仍可使用。若翻譯失敗，請確認 Google API 啟用、金鑰限制、帳單與配額。Basic 版沒有自訂術語表，專有名詞由管理員確認及修改。翻譯僅在後台生成時呼叫；訪客切換語言不會呼叫 Google。

驗證：`node --test tests/admin.test.cjs tests/auto-translation.test.cjs tests/translations.test.cjs` 與 `node tests/admin-browser.test.cjs`。測試使用模擬 Google HTTP 回應搭配真實 handler／SQLite，涵蓋憑證缺漏、欄位對應、錯誤回應、數值驗證、草稿、人工修改保留、上架阻擋及手機版，不會產生 Google 翻譯費用。實際服務需設定 Secret 後再驗證。

## 可讀訂單編號

交易列表顯示固定流水編號（例如 `ORD-000001`）；展開「完整識別碼」可查看原始 UUID。編號在各環境的資料庫內唯一，不因篩選、換頁或訂單狀態而變動，也不保證連號。UUID 仍用於購買與內部資料關聯。

部署此版本前，先對已完成 `0001` 至 `0007` 的 staging 資料庫執行：

```powershell
npx wrangler d1 execute fireside-cards-staging-db --env preview --remote --file=migrations/0008_order_numbers.sql
```

此 migration 依建立時間及 ID 為既有訂單補上編號，新訂單則於同一筆交易內自動取得編號。可重跑而不改變已分配的編號。正式環境上線前也需對正式資料庫套用此 migration。

管理員登入後由導覽列「管理後台」或 `admin.html` 進入。一般會員與未登入者無法呼叫管理 API。所有頁面都以同網域 Session 驗證，不需要另外的管理員密碼。

## 這一版可以做什麼

- 卡牌：搜尋、新增、修改中文名稱／副標／系列／圖片／數值／技能，可編輯外語翻譯。草稿、上架、下架三種狀態，不硬刪除歷史資料。
- 商品：建立卡牌組合，選擇內容卡牌，設定整數金幣售價、上架及下架。
- 交易：依玩家 ID、購買時名稱或 Email、日期與狀態查詢，每頁 25 筆。金額與商品名稱來自訂單快照。
- 每次卡牌／商品新增與修改連同操作者、修改前後資料写入 `admin_audit`，與資料變更同一批交易提交。過期版本會回 409，避免覆蓋他人修改。

圖片欄位目前接受專案內圖片路徑或 HTTPS 圖片網址，沒有提供檔案上傳／圖片儲存服務。原有圖片的數字是圖像的一部分；修改數值仍須準備一致的圖片。

商城顯示站內金幣，不再依語言或 IP 換算貨幣。種子商品預設 30 金幣，可在後台修改。本版沒有錢包、儲值、扣款與發卡服務；購買按鈕標示「購買尚未開放」。交易查詢已可讀取真實 `purchase_orders` 資料，但不會建立示意交易。未來購買服務必須在原子交易中寫入訂單快照、扣除金幣並發卡。

## 資料庫與部署順序

既有資料庫必須已套用 `0001` 至 `0003`。新環境先依檔名順序執行這三份 migration。

先備份正式 D1，先在本機／預覽資料庫驗證，再以相同順序套用正式資料庫：

```powershell
npx wrangler d1 execute fireside-cards-db --local --file=migrations/0004_admin_catalog.sql
npx wrangler d1 execute fireside-cards-db --local --file=migrations/0005_seed_catalog.sql
npx wrangler pages dev .
```

正式資料庫請將 `--local` 改為 `--remote`。後台功能需要完成兩份 migration。若 catalog 資料表尚未建立或尚未匯入任何卡牌，公開 API 會明確標示使用舊資料，首頁及商城會沿用既有 31 張卡牌與三個金幣商品；收藏仍能使用。已初始化的資料庫即使全部卡牌下架，也不會重新顯示舊卡牌。其他資料庫錯誤不會啟用備援。請勿把測試帳號、Cookie 或 `.wrangler` 上傳。

`0005` 匯入現有 31 張卡牌、四語翻譯與三個商品；使用 INSERT OR IGNORE，不覆蓋已在後台編輯的資料。`scripts/build-catalog-seed.cjs` 用於從現有靜態資料重建種子檔，日常新增／編輯請使用後台。原靜態 JS 留作素材與舊工具相容，但線上資料以 D1 為準。

本機資料庫若沒有管理員，先在本機註冊，再以明確帳號的 SQL 更新 `users.role` 為 `admin`。不要在正式資料庫建立測試登入後門。

## 驗證

```powershell
node --test tests/admin.test.cjs tests/favorites.test.cjs tests/translations.test.cjs
node tests/admin-browser.test.cjs
```

後端測試在記憶體 SQLite 執行實際 SQL，包含權限、欄位驗證、併發版本、操作紀錄原子性、上架、金幣价格、玩家搜尋及分頁。瀏覽器測試使用相同 API handler 與臨時 SQLite，從管理表單驗證到公開商城；不連正式資料庫。最終仍應在 Cloudflare 預覽環境確認 D1 綁定與 Functions 路由。

管理 API：`/api/admin/cards`、`/api/admin/products` 支援 GET／POST／PUT；PUT 須帶 id 與讀取時的 version。`/api/admin/orders` 支援 GET 參數 player、from、to、status、page。公開商品目錄為 `/api/catalog`。交易單價與總額以 currency 的整數單位保存，目前新購買規劃使用 COIN。
