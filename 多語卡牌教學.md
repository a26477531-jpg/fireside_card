# 四語卡牌：修改方法與練習

目前有 31 張卡牌，每張包含繁體中文、英文、日文、韓文，共 124 筆翻譯。外語文字是可修改的初稿；網站切換語言是讀取已存好的翻譯，不會即時自動翻譯。

## 資料如何變成卡面

`data/card-translations.csv` → 執行轉換工具 → `translations-data.js` → 網站依卡牌 ID 與語言選出文字。

CSV 是方便編輯的資料表。這個規模不需要另外架設資料庫伺服器。網站載入產生好的 JavaScript，因此不必在瀏覽器裡讀取 CSV，也保留直接開啟 index.html 的方式。

| 欄位 | 用途 |
| --- | --- |
| card_id | 卡牌固定編號，例如 01；不同語言仍使用同一編號 |
| language | zh-TW、en、ja、ko，請維持這些代碼 |
| name | 卡牌名稱 |
| subtitle | 卡牌副標題 |
| ability_1_title / ability_1_text | 第一個技能的標題／規則 |
| ability_2_title / ability_2_text | 第二個技能的標題／規則；只有一個技能就留空 |

`card_id + language` 決定唯一一筆翻譯。名稱可能被翻譯或改名，不能拿名稱當固定識別碼。

圖片、攻擊力、生命、費用與文字框位置仍共用 `cards-data.js`，不需要複製四份。現在修改卡名與顯示規則，請以 CSV 為準；cards-data.js 的中文是備援資料，也仍提供機制篩選所用的中文規則。若改的是技能機制，而不是翻譯措辭，需同步更新這份基礎資料。

## 你可以自己完成的一次修改

1. 用 Excel 或文字編輯器開啟 `data/card-translations.csv`。
2. 找到要改的 card_id 與 language，修改 name 或技能文字，保留欄位名稱。
3. 儲存為 **CSV UTF-8**。Excel 若把 01 變成 1，工具會補回兩位編號。
4. 在此專案資料夾開啟終端機，執行：

```powershell
node scripts/build-translations.cjs
```

5. 看到成功訊息後，重新整理網頁，再選擇對應語言。

不要直接修改 translations-data.js，下次轉換會覆寫它。只儲存 CSV、沒有執行轉換，網站不會取得新文字。

可在 Excel 儲存格使用 Alt+Enter 插入真正的換行。工具支援儲存格內換行、逗號與引號；用純文字編輯器時，需要正確保留 CSV 的引號包覆及跳脫。不要把字面上的 `\n` 當成換行。

## 切換時程式做什麼

語言選單通知 i18n.js 記住語言；app.js 重新取得各卡牌的翻譯，更新卡面、詳情與搜尋結果。localStorage 保存語言偏好，所以重新整理後仍保留選擇。搜尋比對目前語言的文字；切換語言後，如果舊搜尋詞找不到卡片，可以清除搜尋。

機制篩選使用固定的基礎資料，因此「護盾」不會因為翻譯成不同字而漏掉卡片。

轉換工具會拒絕重複的 ID／語言配對、未知語言與不完整技能欄位。每張卡必須有中文；若缺少某個外語的整筆資料，會顯示警告，網站使用中文備援。工具成功驗證後才更新輸出。

## 文字框與圖片

沿用原本逐張校正的名稱、規則位置。card-text-fit.js 會檢查文字是否超出框線或進入下角裝飾區，再視需要縮小字體；CSS 設定是起始大小。長規則仍可點開詳情閱讀，不會刪除內容。

已測試全部 31 張 × 4 種語言 × 2 種卡面寬度（220、360px）的文字邊界。未來換文字或字型後仍應目視確認，尤其是長技能說明。

圖片裡已印好的文字不會跟著翻譯，例如目前輪播圖片的中文。若要改成四語，需提供對應圖片。卡牌圖片上的數字同樣不會因為修改資料而改變。

## 小測驗（先自己想，再回覆我）

1. 只想修改某張卡的日文名稱，要用哪兩個欄位找到那一列？language 應該填什麼？
2. 修改 CSV 後重新整理網站，名稱仍沒變，最可能少做哪一步？
3. 同一張卡的攻擊力為什麼不需要在四種語言的資料裡各存一份？

進階練習：把 01 的英文名稱改成自己的版本，執行轉換，確認中文仍保留原名，再還原英文名稱。
