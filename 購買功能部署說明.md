# 金幣購買與我的卡片

收藏是願望清單，持有卡牌是已購買的卡牌；兩者互不刪除。每次購買一個組合包，包內每種卡牌增加一張，重複購買累計數量。確認視窗顯示本次價格，商品價格或版本變更時拒絕扣款，請重新整理。

## 部署前資料庫準備

新增 `migrations/0007_card_purchases.sql`，需要先有 `0006_coin_balance.sql` 的金幣欄位。0006 先前被 `*.sql` 忽略，這次已修改忽略規則，讓 migrations 可加入 Git。資料庫 migration 不會因為 Git 推送自動套用。

1. `wrangler.toml` 的 Preview DB 已設定為 `fireside-cards-staging-db`（`cb87a9e5-0098-4d80-9696-41374d847ce4`），Production 保留 `fireside-cards-db`。新設定需部署後生效，再於 Cloudflare 確認綁定。
2. 檢查該資料庫 `PRAGMA table_info(users);` 是否已有 `coin_balance`。若沒有，先套用 0006；若有，不要再執行 ALTER TABLE 的 0006。
3. 首次套用購買 migration：`npx wrangler d1 execute fireside-cards-staging-db --env preview --remote --file=migrations/0007_card_purchases.sql`。不要重複執行已套用的 ALTER TABLE migration。
4. 部署本次程式到 staging，登入測試帳號購買；核對金幣扣款、我的卡片張數、收藏保留，以及管理後台的交易搜尋。

已具備 0006 但尚未套用 0007 時，購買 API 回傳 503 與清楚訊息，不會扣款。2026-09-22 使用者回報已在新 staging 資料庫依序完成 0001～0007；不要再次初始化。新資料庫不含正式玩家帳號，需另行註冊測試帳號。

## 實作與範圍

- `POST /api/purchases` 需要登入，使用資料庫價格、商品版本與卡牌狀態驗證，拒絕跨來源請求。每次一包，沒有接受前端提供玩家 ID 或實際扣款金額。
- 同一玩家的 requestId 只能建立一張訂單；連線中斷後重試同一筆不會再扣款。前端把未確認交易的 requestId 存在該分頁的 sessionStorage，重新整理後仍可重試同一商品。
- SQLite trigger 在建立訂單的同一交易內執行扣款、增加持有卡牌、寫入金幣異動。任何一步失敗，整筆回復。
- `coin_transactions.amount` 延續原表的正數規格，`type='purchase'` 表示扣款，`topup` 表示入帳；零價商品不產生金幣異動。
- `purchase_orders.cards_snapshot` 保存交易當時卡牌資料，玩家改名、商品改價或卡牌下架都不會改寫歷史訂單。我的卡片使用取得的資料快照；重複買入更新持有卡牌的展示資料，不改寫舊訂單。
- `GET /api/my-cards` 只回傳登入者自己的卡牌與數量，卡牌下架後仍保留持有紀錄。
- 購買成功可前往獨立 `my-cards.html`，搜尋、查看卡牌詳情、加入或取消收藏。導覽列「我的收藏」連到卡牌庫的收藏篩選。
- 這次不包含轉送、退款或交易玩家卡牌；加值仍是既有作品集模擬流程，沒有真實金流。

## 驗證

`node tests/purchases.test.cjs` 測試權限、價格竄改、重試、並行購買、餘額不足、發卡失敗回復、持有數量、收藏獨立、玩家隔離與下架保留。

`node tests/admin-browser.test.cjs` 使用實際 API handlers 與 SQLite 測試購買、取消、回應遺失後重試、我的卡片、收藏、四語系與手機版。`node tests/admin.test.cjs` 驗證管理 API 與訂單搜尋。
