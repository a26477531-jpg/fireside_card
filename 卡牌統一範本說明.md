# 統一卡牌範本

以 `cards-clean-layout-31/25-cthulhu.webp` 為卡框基準，輸出 31 張 1024 × 1536 的無文字 WebP 至 `cards-unified-31/`。原始檔案不覆寫；各卡插畫裁切、等比例縮放後放入同一開孔，周邊少量背景會被裁掉。

## 固定座標

唯一設定來源為 `card-template.json`。下表都是相對於左上角的像素座標，網頁轉為百分比，所以不同顯示尺寸的位置一致。

| 欄位 | x | y | 寬 | 高 |
|---|---:|---:|---:|---:|
| 魔力消耗 | 53 | 71.5 | 150 | 125 |
| 攻擊力 | 64 | 1304.5 | 146 | 125 |
| 血量 | 816 | 1291.5 | 150 | 125 |
| 名稱 | 155 | 835 | 714 | 90 |
| 規則 | 100 | 975 | 824 | 350 |

規則區下方 30% 的左右各 22% 避開數值徽章。文字太長時縮小字級，欄位不移動。已驗證現有 31 張卡牌的中、英、日、韓文，以及三位數 999；新增更長內容仍需預覽確認。84px 商城縮圖的文字只適合辨識排版，閱讀完整規則應開啟詳情。

## 網頁與後續新增

- 首頁、輪播、商城及詳情透過共用 `card-artwork.js` 使用範本。
- 舊資料庫的 31 個圖片路徑會透過 `card-template.js` 對應到新圖片，不需要修改資料庫。數值與文字仍由卡牌資料填入。
- 新卡牌要使用同一框，請先產生相同規格圖片並放入 `cards-unified-31/`，再把該路徑填入卡牌資料；任意其他圖片路徑維持原本排版，不會自動替圖片換框。
- `output/card-template/cthulhu-frame.png` 是可重用的透明插畫開孔卡框。
- 維持原本首頁與商城排版。三個數值對齊徽章視覺中心，並補償數字字型的基線偏移。
- `card-template-review.html` 是獨立的排版檢查頁，可直接開啟，切換語言、尺寸、定位框及 999 數值測試，不會取代首頁或商城。

## 重建與驗證

使用有 Pillow 的 Python 執行 `python scripts/build-unified-cards.py`，另需 Node.js 讀取卡牌資料。腳本會重建圖片與 `card-template.js`，並逐像素驗證插畫開孔以外的區域與參考圖完全一致。

使用有 Playwright 的 Node.js 執行 `node tests/card-template.test.cjs`；可用 `PLAYWRIGHT_MODULE` 指定 Playwright 安裝位置。測試使用 Edge，檢查 31 張 × 4 語言 × 4 尺寸 × 2 數值情境，共 992 組排版，確認五個欄位的座標和文字邊界。

目前使用無損 WebP，31 張合計約 50 MiB，適合保留精準卡框；公開頁面已有圖片延遲載入。
